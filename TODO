TODO:
* support sending announce_peer packets
* Add responding to queries support
* proper input validation / error handling everywhere
* move dht, upnp options into the configuration file
* Add indicator if not receiving any incoming queries (works?)
